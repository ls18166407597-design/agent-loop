# 阶段 01：项目搭建

## 目标
搭建项目基础架构，确保项目能启动运行。

## 具体任务
- 检查项目目录结构是否合理
- 确认 package.json / 依赖安装正常
- 运行 `npm install` 确保依赖完整
- 检查 .env 配置是否完整（如有）
- 运行项目确认能启动（`npm run dev` 或 `npm start`）
- 如果有数据库，确认迁移/初始化正常

## 成功标准
- `npm install` 无报错
- 项目能正常启动
- 无致命错误

## 时间限制
20 分钟
